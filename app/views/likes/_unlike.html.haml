= link_to'<i class="glyphicon glyphicon-thumbs-down btn btn-default"></i>'.html_safe, image_like_path(@image.id, @like.id), :method => :delete, remote: true
%span.likes-count
  = @image.likes.size
  = t('.likes')
